{
    "stripe": {
        "command": "npx",
        "args": [
            "-y",
            "@stripe/agent-toolkit",
            "--tools=all"
        ],
        "env": {
            "STRIPE_SECRET_KEY": "${STRIPE_SECRET_KEY}"
        }
    }
}